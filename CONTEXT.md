# Forge AI 标注与训练领域

本项目管理工业质检数据从导入、处理、标注、审核到模型训练和产物交付的完整流程。本文统一业务语言，避免把文件、帧、任务、Job 和标注版本混为一谈。

## 身份与权限

**管理员**：平台运维和业务管理身份，可管理用户、系统设置、数据资源、标注任务、审核、训练、模型和下载权限。

**审核员**：负责检查和修正标注、处理审核队列，并可执行模型训练、数据和模型下载；不能管理用户、系统设置或删除模型。

**标注员**：负责领取分配给自己的标注 Job、编辑标注草稿并提交审核；不能浏览或下载未授权的完整数据。

**多角色用户**：同一用户可拥有多个固定身份，实际权限取所有身份的并集；不要求切换当前身份。

## 数据与处理

**资源（Asset）**：用户导入的平台原始对象，可以是单张图片、图片压缩包或视频。

**帧（Frame）**：由视频按处理配置生成的、可被标注的图片资源；每帧保留原始帧号、抽取序号和时间戳。

**处理批次（Processing Run）**：一次由管理员发起的数据处理版本，记录输入资源、抽帧配置、分段配置、处理结果和错误。已有标注的处理结果不可被静默替换。

**标注任务（Annotation Task）**：一个数据集默认拥有的标注工作容器，冻结任务级标签和属性模板，并包含多个 Segment。

**Segment**：标注任务中的连续工作分段。图片默认每 100 张为一个 Segment，视频默认每 100 帧为一个 Segment；Segment 不跨越视频资源。

**Job**：一个可领取、编辑、提交和审核的最小工作单元；当前模型中一个 Segment 对应一个 Job。

## 标注与审核

**标签（Label）**：任务级定义的业务类别，例如裂纹或划痕；任务开始处理后冻结。

**属性（Attribute）**：标签或 Shape 的结构化特征，支持枚举、布尔、整数和文本类型，由管理员在任务级模板中定义。

**Shape**：某个图片或视频帧上的具体几何标注，支持矩形、多边形、折线、椭圆、关键点和骨架。

**Track**：跨视频帧表示同一目标的轨迹；第一期支持矩形框轨迹、关键帧和插值帧。

**草稿（Draft）**：自动保存但尚未提交审核的标注内容。

**提交（Submission）**：标注员完成 Job 后发起的正式审核请求；提交后 Job 锁定。

**返工（Rework）**：审核驳回后的 Job 状态，默认回到原标注员，重新提交后再次进入审核。

**审核快照（Review Snapshot）**：审核员修改标注时保存的修改前后完整 JSON、操作者、时间、操作类型和审核意见；当前标注内容可以覆盖保存，但历史必须可追溯。

## 模型与数据交付

**训练快照（Training Snapshot）**：创建训练任务时固化的 Job/Segment、资源版本、最终审核标注、标签模板、数据划分和处理配置摘要；训练只读取快照。

**产物（Artifact）**：导出包、训练权重或模型转换文件等受管文件及其数据库元数据，不等同于原始资源。

**正式训练数据**：默认是当前数据集全部审核通过的 Job/Segment，也允许管理员或审核员手动选择已审核通过的子集。

